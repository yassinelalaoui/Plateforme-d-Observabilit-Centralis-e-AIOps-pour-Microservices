{
  "name": "my-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:contract": "vitest run --config order-service/vitest.config.ts tests/contract",
    "test:integration": "vitest run --config order-service/vitest.config.ts tests/integration"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^4.1.10"
  }
}
